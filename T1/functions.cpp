#include <iostream>
#include "functions.hpp"

using namespace std;

void start(){
    int size;
    cin >> size;
    int array[size];

    for(int i = 0; i < size; ++i) {
        cin >> array[i];
    }
}